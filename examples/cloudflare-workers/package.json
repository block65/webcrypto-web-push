{
  "name": "cloudflare-workers-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler d1 migrations apply db --local && wrangler dev main.ts --port=3065",
    "generate": "drizzle-kit generate --config drizzle.config.ts",
    "test": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "workspace:*",
    "drizzle-orm": "1.0.0-rc.4",
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "@tsconfig/node26": "^26.0.1",
    "drizzle-kit": "1.0.0-rc.4",
    "wrangler": "^4.128.0"
  }
}
